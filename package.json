{
  "name": "overlay-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute overlay-db --file=schema.sql",
    "db:init:remote": "wrangler d1 execute overlay-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
